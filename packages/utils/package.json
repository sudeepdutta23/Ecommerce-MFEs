{
  "name": "@ecom/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared runtime helpers: typed event bus, API client, auth/session storage, formatters. Framework-agnostic — no React.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ecom/types": "workspace:*",
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
